import Layout from "@/components/Layout/layout";

const Settings = () => {
    return (  
        <Layout>
            <div className="flex h-screen w-screen">Settings</div>
        </Layout>
    );
}
 
export default Settings;